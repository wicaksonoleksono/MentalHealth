server {
    listen 2121;

    location / {
        include proxy_params;
        proxy_pass http://unix:/var/www/mlmh/flaskapp.sock;
    }
    # location /static {
    #     alias /var/www/mlmh/app/static; # Adjust path to your static files
    # }
}